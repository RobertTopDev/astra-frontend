apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  namespace: {{ .Values.backendConfig.namespace }}
  name: astra-backendconfig
spec:
  securityPolicy:
    name: {{ .Values.backendConfig.securityPolicy.name }}
  sessionAffinity:
    affinityType: "CLIENT_IP"
  timeoutSec: 30