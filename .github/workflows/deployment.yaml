name: Code Quality Gate - SonarQube

on:
  push:
    branches:
      - mainnet # prod
      - staging 
      - testnet # alpha
      - feature/**
  pull_request:
    types: [opened, edited, reopened, synchronize]
    branches:
      - mainnet
      - staging
      - testnet
      - feature/**

jobs:
  sonar-scan:
    runs-on: ubuntu-latest

    steps:
      - name: "Checkout repository on branch: ${{ github.REF }}"
        uses: actions/checkout@v4
        with:
          ref: ${{ github.HEAD_REF }}
          fetch-depth: 0

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: SonarCloud Scan
        uses: sonarsource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  write-creds-from-secrets:
    name: "Write env from secrets"
    runs-on: ubuntu-latest
    needs: [sonar-scan ]

    steps:
      - name: Checkout branch
        uses: actions/checkout@v4

      - name: Get branch names.
        id: branch-names
        uses: tj-actions/branch-names@v8
      - name: Write .env.production from secrets
        run: |
          echo "${{ secrets.ENV }}" > .env
        env:
          ENV: ${{ secrets.ENV}}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: creds-artifact
          path: |
            .env

  docker-build-and-push:
    name: "Docker Build and Push"
    runs-on: ubuntu-latest
    needs: [sonar-scan, write-creds-from-secrets]
    
    steps:
      - uses: actions/checkout@v4

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: creds-artifact
          path: .

      - name: Get branch names.
        id: branch-names
        uses: tj-actions/branch-names@v8

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GKE_SA_KEY }}

      - name: Build docker image
        run: |
          gcloud auth configure-docker us-central1-docker.pkg.dev
          docker build -t us-central1-docker.pkg.dev/tmgalaxy/astra-testnet/astra-frontend:testnet-${GITHUB_SHA::7} .

      - name: Push image to dev repo if branch is development
        if: (github.ref == 'refs/heads/testnet') && (github.event.pull_request.merged == true || github.event_name == 'push')
        run: |
          docker push us-central1-docker.pkg.dev/tmgalaxy/astra-testnet/astra-frontend:testnet-${GITHUB_SHA::7}

  release-to-k8s:
    name: "Release to k8s"
    runs-on: ubuntu-latest
    needs: [docker-build-and-push]

    steps:
      - uses: actions/checkout@v4
      - name: Authenticate to Google Cloud

        uses: google-github-actions/auth@v2
        with:
            credentials_json: ${{ secrets.GKE_SA_KEY }}

      - name: Get branch names.
        id: branch-names
        uses: tj-actions/branch-names@v8

        # get alpha cluster creds
      - name: "Get GKE alpha credentials"
        if: ${{ steps.branch-names.outputs.ref_branch != 'mainnet' }}
        uses: "google-github-actions/get-gke-credentials@v2"
        with:
            cluster_name: "cluster-alpha-us-central1"
            location: "us-central1"

      - name: Deploy to cluster
        if: (github.ref == 'refs/heads/mainnet' || github.ref == 'refs/heads/testnet') && (github.event.pull_request.merged == true || github.event_name == 'push')
        run: |
            helm upgrade --install astra-frontend ./helm/astra-frontend \
            --namespace astra-testnet \
            --values ./helm/values.yaml \
            --set image.tag=testnet-${GITHUB_SHA::7} \
            --timeout 5m0s --create-namespace --debug








