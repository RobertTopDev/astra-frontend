import Completed from '@/app/launchpad/(page)/components/Completed'

export const CompletedLaunchpad = () => {
  return (
    <div className="container pb-20" id="launchpad">
      <Completed type="dashboard" />
    </div>
  )
}
